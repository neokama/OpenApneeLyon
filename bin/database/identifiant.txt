1ère lettre du prenom
2 premières lettres du nom
numéro


CREATE TABLE competiteurs (id INTEGER PRIMARY KEY AUTOINCREMENT, prenom varchar(30), nom varchar(30), sexe CHARACTER(1), num_license varchar(30), equipe varchar(30), epreuve1 varchar(30), annonce1 INTEGER, epreuve2 varchar(30), annonce2 INTEGER)
CREATE TABLE classement (id INTEGER PRIMARY KEY AUTOINCREMENT, prenom varchar(30), nom varchar(30), sexe CHARACTER(1), equipe varchar(30), epreuve varchar(30), resultat INTEGER)
CREATE TABLE classementequipe (id INTEGER PRIMARY KEY AUTOINCREMENT, equipe varchar(30), point INTEGER)

	/*
	tableau de compétiteurs
	trier tableau par annonces
	Voir fichier config: passages simultanés
	Calcul heure de passage (Echau => annonce => )

	t-33 => Echauffement
	t-3 => annonce
	t => Passage 
	t + annonce max + seuil max + marge série => t2
	Exportation planning
	*/